README
See Dockerfile for Ruby version.
Rails and Bundler version inside Gemfile.

Deployment instructions:

Navigate to root of application. Run the following commands.
$ gem install bundle
$ bundle install
$ rake db:create db:migrate db:seed
$ rails server
