== README
See Gemfile for ruby, rails and bundle version details.

* Deployment instructions
Navigate to root of application. Run the following commands.
$ gem install bundle
$ bundle install
$ rake db:create db:migrate db:seed
$ rails server
